# nuxt_agbi_template

This is a custom template for initial Nuxt.js projects

It's has configure:
 - the src/ as the root source folder (for use ~ instead of ~~)
 - SASS/SCSS scalable folder architecture
 - And an idea for only one plugin file, which should contain all mounts 3rd party vue components (following the Mirum idea)